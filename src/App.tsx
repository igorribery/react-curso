import { useState } from "react";
import './styles.css';

const App = () => {
  
  const handleClick = () => {
    
  }
  return (
    <div>
      <button className='botao' onClick={handleClick}>Clique Aqui</button>
    </div>
  );

}

export default App;